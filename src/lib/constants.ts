/** Marks the single "creator profile" row in `jobs` so it's excluded from the
 *  normal random draw and only ever reachable through Meet the Creator. */
export const CREATOR_TAG = "creator-profile";